<form method="post" action="{{ path('app_a_client_entity_delete', {'id': a_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_client.id) }}">
    <button class="btn">Delete</button>
</form>
