{% extends 'base.html.twig' %}

{% block title %}AClient{% endblock %}

{% block body %}
    <h1>AClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ a_client.id }}</td>
            </tr>
            <tr>
                <th>Cnom</th>
                <td>{{ a_client.cnom }}</td>
            </tr>
            <tr>
                <th>Cprenom</th>
                <td>{{ a_client.cprenom }}</td>
            </tr>
            <tr>
                <th>Ctele</th>
                <td>{{ a_client.ctele }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_a_client_entity_index') }}">back to list</a>

    <a href="{{ path('app_a_client_entity_edit', {'id': a_client.id}) }}">edit</a>

    {{ include('a_client_entity/_delete_form.html.twig') }}
{% endblock %}
