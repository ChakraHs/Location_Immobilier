<form method="post" action="{{ path('a_reservation_delete', {'id': a_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_reservation.id) }}">
    <button class="btn">Delete</button>
</form>
